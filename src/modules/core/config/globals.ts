import { Roboto_Flex, Roboto_Mono } from "next/font/google";

export const roboto = Roboto_Flex({ subsets: ['latin'] });
export const roboto_mono = Roboto_Mono({ subsets: ['latin']});
export const metadata = {
    title: 'Space Industries',
    description: 'THE number 1 web browser MMORPG in recent times -- One Stone',
}